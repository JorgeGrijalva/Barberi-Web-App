# ... existing code ...
# Eliminar las siguientes dos líneas
# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# Puedes agregar un comando que no falle si no hay pruebas especificadas
echo "No hay pruebas especificadas, omitiendo..."
# ... existing code ...
